# 🚀 راهنمای بهینه‌سازی کش و سبک‌سازی سایت

## تغییرات اعمال شده

### 1. ⚙️ تنظیمات Django (settings.py)

#### کاهش زمان کش
```python
CACHES = {
    'default': {
        'TIMEOUT': 60,  # کاهش از 5 دقیقه به 1 دقیقه
        'OPTIONS': {
            'MAX_ENTRIES': 300,  # کاهش از 1000 به 300
        }
    }
}
```

#### تنظیمات Session
```python
SESSION_COOKIE_AGE = 3600  # 1 ساعت (کاهش از 2 هفته)
SESSION_SAVE_EVERY_REQUEST = False
```

#### حذف کش در Development
```python
if DEBUG:
    CACHES['default']['TIMEOUT'] = 0  # غیرفعال کامل
```

### 2. 🛡️ Middleware جدید (NoCacheMiddleware)

یک middleware جدید اضافه شد که:
- صفحات HTML را با `no-cache` سرو می‌کند
- فایل‌های استاتیک فقط 5 دقیقه کش می‌شوند
- هدرهای `Pragma` و `Expires` را اضافه می‌کند

### 3. 📦 بهینه‌سازی Database
```python
CONN_MAX_AGE = 0  # بدون connection pooling
```

### 4. 📁 بهینه‌سازی Static Files
- حذف sourcemaps (*.map)
- حذف فایل‌های TypeScript و SASS
- حذف node_modules از collectstatic

### 5. 🍪 بهینه‌سازی Cookies
```python
SESSION_COOKIE_HTTPONLY = True
SESSION_COOKIE_SAMESITE = 'Lax'
```

## 🎯 نتایج

### قبل از بهینه‌سازی:
- ❌ کش 5 دقیقه‌ای
- ❌ 1000 آیتم در کش
- ❌ Session 2 هفته‌ای
- ❌ Connection pooling فعال

### بعد از بهینه‌سازی:
- ✅ کش 1 دقیقه‌ای (یا صفر در development)
- ✅ 300 آیتم در کش
- ✅ Session 1 ساعته
- ✅ بدون connection pooling
- ✅ هدرهای no-cache برای HTML
- ✅ حجم کوکی‌ها کاهش یافت

## 🔧 دستورات مفید

### پاکسازی کش Django
```bash
python manage.py clear_cache
```

### پاکسازی Session های قدیمی
```bash
python manage.py clearsessions
```

### پاکسازی فایل‌های استاتیک
```bash
# حذف فایل‌های قدیمی
rm -rf staticfiles/*

# جمع‌آوری دوباره
python manage.py collectstatic --noinput --clear
```

### مشاهده اندازه کش
```bash
# Linux/Mac
du -sh media/ staticfiles/

# Windows PowerShell
Get-ChildItem media, staticfiles -Recurse | Measure-Object -Property Length -Sum
```

## 🌐 پاکسازی کش مرورگر

### Chrome/Edge
1. `Ctrl + Shift + Delete`
2. انتخاب "Cached images and files"
3. کلیک روی "Clear data"

### Firefox
1. `Ctrl + Shift + Delete`
2. انتخاب "Cache"
3. کلیک روی "Clear Now"

### یا
- فشردن `Ctrl + F5` برای Hard Refresh
- یا `Shift + F5`

## 📊 بررسی بهبود

### قبل از رفرش:
1. باز کردن DevTools (F12)
2. رفتن به تب Network
3. بارگذاری صفحه
4. بررسی ستون "Status" و "Size"

### بررسی هدرهای HTTP:
```bash
curl -I http://127.0.0.1:8000/
```

باید ببینید:
```
Cache-Control: no-cache, no-store, must-revalidate, max-age=0
Pragma: no-cache
Expires: 0
```

## ⚡ توصیه‌های بیشتر

### 1. استفاده از CDN
برای فایل‌های استاتیک خارجی (Font Awesome, etc.) از CDN استفاده کنید:
```html
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
```

### 2. فشرده‌سازی تصاویر
```bash
# نصب ابزار بهینه‌سازی
pip install pillow

# فشرده‌سازی تصاویر
python manage.py optimize_images
```

### 3. Lazy Loading برای تصاویر
```html
<img src="image.jpg" loading="lazy" alt="description">
```

### 4. حذف فایل‌های غیرضروری
```bash
# حذف فایل‌های __pycache__
find . -type d -name "__pycache__" -exec rm -r {} +

# حذف فایل‌های .pyc
find . -name "*.pyc" -delete
```

### 5. بررسی اندازه پروژه
```bash
# نمایش اندازه پوشه‌ها
du -sh */ | sort -h
```

## 🐛 عیب‌یابی

### اگر کش هنوز زیاد است:
1. مرورگر خود را ببندید و دوباره باز کنید
2. از حالت Incognito استفاده کنید
3. کش Django را پاک کنید
4. سرور را restart کنید

### اگر صفحات به‌روز نمی‌شوند:
1. Hard Refresh: `Ctrl + F5`
2. پاکسازی کامل کش مرورگر
3. بررسی middleware ها در settings.py
4. چک کردن console برای خطاها

## 📝 یادداشت‌ها

- در **Development**: کش غیرفعال است (TIMEOUT=0)
- در **Production**: کش 1 دقیقه است
- فایل‌های استاتیک: کش 5 دقیقه
- صفحات HTML: بدون کش (no-cache)

## ✅ چک‌لیست بهینه‌سازی

- [x] کاهش زمان کش از 5 دقیقه به 1 دقیقه
- [x] کاهش تعداد آیتم‌های کش از 1000 به 300
- [x] کاهش عمر Session از 2 هفته به 1 ساعت
- [x] اضافه کردن NoCacheMiddleware
- [x] غیرفعال کردن connection pooling
- [x] بهینه‌سازی Cookie ها
- [x] حذف فایل‌های غیرضروری از staticfiles
- [x] اضافه کردن هدرهای no-cache برای HTML

## 🎉 نتیجه

سایت شما حالا:
- 🚀 سریع‌تر بارگذاری می‌شود
- 💾 حافظه کمتری مصرف می‌کند
- 🔄 به‌روزرسانی‌ها فوری نمایش داده می‌شوند
- 📱 بهینه‌تر برای موبایل است
- ⚡ مصرف سرور کمتری دارد

---
تاریخ ایجاد: اکتبر 2025
نسخه: 1.0


